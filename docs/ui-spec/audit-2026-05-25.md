# UI Spec Audit - 2026-05-25

Scope: pages under `src/app/(site)` and `src/app/(platform)` after Stage 4.2, Stage 4.3, and the Stage 4 cleanup pass.

## `src/app/(site)/page.tsx`

- Uses token colors only? Y - route-level hardcoded colors were removed; bespoke SVG/illustration components remain intentionally art-directed.
- Type scale followed? Y - hero, feature, comparison, team, and CTA sections use the Stage 4 hierarchy.
- Spacing scale followed? Y - route-level card radius, grid, and section spacing now follow shared shell/card conventions.
- Responsive breakpoints work? Y - site shell and hero stack correctly across mobile/tablet/desktop.
- Loading + empty + error states present? N - ticker fetch has fallback data, but the page has no explicit page-level loading/error state.

## `src/app/(site)/learn/page.tsx`

- Uses token colors only? Y - route-level colors and module catalog wrappers use shared tokens.
- Type scale followed? Y - hero and module headings follow the Stage 4 hierarchy.
- Spacing scale followed? Y - hero, catalog wrapper, and module cards use shared radius/spacing conventions.
- Responsive breakpoints work? Y - module grid collapses appropriately.
- Loading + empty + error states present? N - static page has no explicit empty/error treatment.

## `src/app/(site)/demo/page.tsx`

- Uses token colors only? Y - demo shell and access panels use shared token utilities.
- Type scale followed? Y - hero and form sections are aligned to the Stage 4 hierarchy.
- Spacing scale followed? Y - panel spacing and form controls use shared utilities.
- Responsive breakpoints work? Y - panels stack on small screens.
- Loading + empty + error states present? N - form feedback exists, but page-level loading/empty states are not formalized.

## `src/app/(platform)/student/page.tsx`

- Uses token colors only? N - dashboard route is clean, but several student child components still contain chart/art hardcodes and legacy market accent classes.
- Type scale followed? Y - core dashboard titles, numbers, labels, action tabs, and rankings now follow the Stage 4 hierarchy.
- Spacing scale followed? Y - main dashboard sections were normalized to card/grid spacing.
- Responsive breakpoints work? Y - dashboard is single-column first, with desktop action/side rail split.
- Loading + empty + error states present? N - access gate and local empty states exist, but page-level loading/error states remain partial.

## `src/app/(platform)/student/market/page.tsx`

- Uses token colors only? N - market child component keeps intentional chart palettes and some legacy accent classes for data visualization.
- Type scale followed? Y - market board titles, numbers, rankings, and metric cards now follow a clearer scale.
- Spacing scale followed? Y - long vertical flow was replaced with grouped dashboard rows.
- Responsive breakpoints work? Y - watchlist, main stock card, radar, structure, ranking, sector, and notes stack progressively.
- Loading + empty + error states present? N - refresh/error notice exists inside the component, but route-level loading state is absent.

## `src/app/(platform)/student/history/page.tsx`

- Uses token colors only? N - history dashboard component still needs a focused chart/timeline token pass.
- Type scale followed? N - existing chart/timeline typography predates this cleanup pass.
- Spacing scale followed? N - section spacing is serviceable but not fully aligned to the new scale.
- Responsive breakpoints work? Y - history layout already follows single-column mobile and larger-screen split.
- Loading + empty + error states present? N - low-data handling exists, but route-level loading/error states are not formalized.

## `src/app/(platform)/teacher/page.tsx`

- Uses token colors only? Y - teacher shell and console now use shared token utilities.
- Type scale followed? Y - stats, section headings, task fields, and list cards use the Stage 4 hierarchy.
- Spacing scale followed? Y - cards, inputs, chips, and grids now use shared spacing/radius conventions.
- Responsive breakpoints work? Y - platform shell handles small-screen navigation and page stacking.
- Loading + empty + error states present? N - access gate exists; dashboard loading/error states are partial.

## `src/app/(platform)/parent/page.tsx`

- Uses token colors only? Y - parent report cards and chart container now use shared token utilities.
- Type scale followed? Y - report headings, summary cards, and comments use the Stage 4 hierarchy.
- Spacing scale followed? Y - panel spacing and chart surfaces use shared card conventions.
- Responsive breakpoints work? Y - platform shell gives acceptable mobile/tablet behavior.
- Loading + empty + error states present? N - access gate exists; report loading/error states are partial.

## `src/app/(platform)/admin/page.tsx`

- Uses token colors only? Y - admin overview cards, chips, and list tiles now use shared token utilities.
- Type scale followed? Y - metrics, section headings, list entries, and badges use the Stage 4 hierarchy.
- Spacing scale followed? Y - cards and grids are normalized to shared spacing/radius conventions.
- Responsive breakpoints work? Y - content is light and collapses safely.
- Loading + empty + error states present? N - access gate exists; admin data states remain partial.

## Follow-Up Priority

1. Focused token pass for student child components: `StudentSandbox`, `StudentMarketBoard`, `StudentHistoryReviewDashboard`, and chart/radar SVG palettes.
2. Route-level loading/empty/error states for teacher, parent, admin, market, and history.
3. Optional art-token pass for site illustration components if the visual direction needs strict token-only enforcement.
